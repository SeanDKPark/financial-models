# Financial Engineering Models

A modular pricing and curve bootstrapping library.

## Documentation

- [Architecture](docs/architecture.md) — module layers, class hierarchy, and the
  market-data → bootstrap → curve → valuation data flow (with Mermaid diagram).
  Regenerate with the `/update-architecture` slash command.
